# Implementation Plans

Generated by the improve/deep architecture audit on 2026-06-14. Execute in the order below unless dependencies say otherwise.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Replace prompt inputs with inline editor popovers | P1 | M | - | TODO |
| 002 | Split PDF operation writers from PdfEngine | P2 | M | - | TODO |
| 003 | Enforce exhaustive operation dispatch (writer + overlay `never`) | P1 | M | 002 (rec.) | TODO |
| 004 | Make the drag/resize hot path cheap (throttle + memoize) | P2 | M | - | TODO |
| 005 | Make TXT/CSV/XLSX export reflect edits | P2 | M | - | TODO |
| 006 | Strengthen PDF write-path + export-dispatch test coverage | P2 | M | - | TODO |

## Deep audit 2026-06-17 — shipped directly (operator-authorized)

A third deep audit (all nine categories, parallel Opus subagents) was run on
2026-06-17 against commit `8c2aec8` on branch `improve/deep-audit-fixes`. Each
finding below was vetted against the cited code, then implemented directly on the
branch and verified with typecheck + lint + unit tests (45 → 48) + build + e2e
(13 Playwright tests). Higher-risk/larger findings became plans 003–006 instead.

| Finding | Title | Category | Status |
|---------|-------|----------|--------|
| SEC-01 | CSV/XLSX formula neutralization also covers a leading line-feed (`\n`) | security | DONE |
| SEC-02 | Inspector clears an unsafe link URL on edit instead of retaining it | security | DONE |
| SEC-03 | Add `Strict-Transport-Security` (HSTS) header in `vercel.json` | security | DONE |
| BUG-02 | Dragging an ink/draw stroke now translates its points, not just the box | bug | DONE |
| BUG-03 | PNG export clears the selection first so editor chrome isn't baked in | bug | DONE |
| BUG-06 | Duplicating replacement text no longer re-masks the original location | bug | DONE |
| BUG-09 | `downloadBlob` defers `revokeObjectURL` so large downloads aren't aborted | bug | DONE |
| DEBT-06 | Delete dead `src/components/Importer.tsx` | tech-debt | DONE |

## Deep audit 2026-06-15 — shipped directly (batch A–J)

A second deep audit (all nine categories) was run on 2026-06-15. The following
"deploy-ready & safe" batch was implemented directly on branch
`akash/lumen-sketch-landing` (operator-authorized), verified with
typecheck + lint + unit tests + build, for review and merge to `main`.

| Item | Title | Category | Status |
|------|-------|----------|--------|
| A | Vercel deploy config (`vercel.json`), `.nvmrc`, `engines`, pin `pdfjs-dist`, remove `yarn.lock` | dx/deps | DONE |
| B | CSV/XLSX formula-injection neutralization | security | DONE |
| C | Link URL sanitization (create/edit/export) via `src/utils/url.ts` | security | DONE |
| D | Import hardening: size + magic-byte validation via `src/utils/fileValidation.ts` | security | DONE |
| E | CSP + security headers (in `vercel.json`) | security | DONE |
| F | Quality gates: `typecheck`/`lint`/`format` scripts, ESLint + Prettier, GitHub Actions CI | dx | DONE |
| G | Correctness quick-wins: exhaustive reducer `never`, safe shape fill, `atob` guard, exhaustive export switch, drag clamp + pointer capture, image `.catch`, font-size clamp, `<img>` data-URL guard | bug | DONE |
| H | Destroy PDF.js documents after load/extract | bug | DONE |
| I | Characterization tests for `savePdf` + export injection + url/file/download utils (17 → 40 tests) | tests | DONE |
| J | README correction (no SheetJS) + new `AGENTS.md` | docs | DONE |

## Dependency notes

- 001 and 002 are independent. 001 improves product polish; 002 improves extension locality for future tools.
- The batch A–J above is independent of 001/002. Recommended next: execute 002 (it now has
  `tests/pdfEngineSave.test.ts` as a characterization backstop) before broader export refactors.
- 003 (exhaustive dispatch) is best landed after 002 (writer split) but can stand alone.
- 006 (write-path tests) complements 003 and 005 — landing 006 first gives those refactors a
  stronger regression backstop, but it is not a hard prerequisite.

## Deferred (from 2026-06-15 audit) — not in the shipped batch

- View-rotation overlay coordinate desync (`App.tsx:381`, `PdfCanvas.tsx:503`) — MED risk.
- `contentEditable` text editing cursor jumps (`OperationOverlay.tsx`) — MED.
- Autosave races + swallowed errors (`App.tsx:153-159`) — S/M.
- Performance: lazy-load export libs/pdf-lib, `PageRail` virtualization, throttle drag dispatch, `React.memo` hot paths, single shared `Document`.
- Tech-debt: operation-registry refactor (extend 002), god-file splits (`PdfCanvas`/`App`), dedupe `hexToRgb`/font-name helpers, `Partial<EditOperation>` patch typing, dead-code removal (`Importer.tsx`, unused exports).
- Direction features: cloud/URL import, finish ink/draw + drawn signatures, edit-aware TXT/CSV/XLSX, real AcroForm fields.

## Deferred (from 2026-06-17 audit) — vetted findings turned into plans or left for later

Turned into plans: **003** (BUG-01-adjacent exhaustive dispatch / DEBT-01), **004**
(PERF-01/02/03 drag hot path), **005** (DIR-02 edit-aware data export), **006**
(TEST-01/02 write-path coverage). Left as future work (vetted, not yet planned):

- **BUG-01** Overlay/page-stage ignore page rotation (`coordinates.ts`, `PdfCanvas.tsx:665`) — L, MED risk; the long-standing rotation desync. Highest-value remaining bug; deserves its own plan.
- **BUG-04** Page insert/delete/rotate mints a new session fingerprint each time, churning IndexedDB (`useEditorController.ts:264`) — S/M.
- **BUG-05** Page structure changes wipe undo/redo history (`useEditorController.ts:266`) — M.
- **BUG-07** Multi-line text flattened on edit (`textContent` vs `innerText`, `OperationOverlay.tsx:114`) — S/M.
- **BUG-08** Inspector font-size change doesn't grow the text box like the toolbar does — S.
- **PERF-04** `PageRail` thumbnails have no virtualization; **PERF-05** export/pdf-lib libs not code-split; **PERF-06/07** PDF parsed/copied 3–4× per load; **PERF-08** autosave/session listing reads full bytes.
- **DEBT-01** lives in plan 003. **DEBT-02/03** dedupe color + font-name helpers (S); **DEBT-04** `as Partial<EditOperation>` cast cluster (M); **DEBT-05** split `PdfCanvas` god file (L); **DEBT-07** reconcile the two text editors (Inspector vs FloatingOperationToolbar) (M); **DEBT-08** consolidate PDF.js worker setup behind the engine (S).
- **SEC-04** CSP `style-src 'unsafe-inline'` hardening — MED, largely by-design for React inline styles.
- **DX-01/DX-02/DOC-01** CI does not run the `e2e` or `format:check` gates that AGENTS.md/README call mandatory (`.github/workflows/ci.yml`) — S; either wire them or soften the docs.
- **DEP-01** react-pdf 9 + pdfjs-dist 4.8.69 are a coupled 2-major migration (defer; `npm audit` clean); **DEP-02** `tailwind-merge` is dead weight (no Tailwind) — S.
- **DIR-01** Forms export as flat graphics, not real AcroForm fields (`pdfEngine.ts:589`); **DIR-03** signature `drawn`/`image` modes are dead enum branches; **DIR-05** cloud/Web-URL import (shipped-but-disabled UI).

## Findings considered and rejected

- Replacing `fflate` with a full spreadsheet library: rejected for v1 because the current export only needs a single inline-string worksheet, and `npm audit --audit-level=moderate` is clean with the small writer.
- Clearing `selectedId` on page delete (audit suggested it as a bug): rejected — `loadPdfState` already dispatches `reset`, which clears `selectedId`, so there is no stale-selection bug on the page-delete path.
- 2026-06-17 — Converting `savePdf`/`OperationOverlay` to switches was **not** shipped in the direct batch (only planned as 003): it is MED-risk (draw-order regressions on `text + whiteout`) and wants the plan-002 writer split first, so it was left for review rather than landed unreviewed.
- 2026-06-17 — `npm audit` re-run: **0 vulnerabilities**; no dependency security finding to act on. The pdfjs-dist 2-major lag is intentional coupling with react-pdf 9, not drift (DEP-01, deferred).
